name: Pipeline
on: [push]

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Roda um comando!
        run: echo $(pwd)
      
      - name: Roda outro comando!
        run: echo $(ls)
